# Example Environment Variables for retro_template

# --- Database Configuration ---
# Set DB_TYPE to "postgres", "sqlite", or "none" (or leave empty) to disable.
DB_TYPE=sqlite

# Connection string or path.
# - For postgres: postgres://user:password@localhost:5432/postgres?sslmode=disable
# - For sqlite: Path to the database file (e.g., local/db/transaction_logs.db)
DB_CONNECTION_STRING=local/db/transaction_logs.db

# Maximum number of connections in the PostgreSQL pool (ignored if DB_TYPE is not postgres)
DB_POOL_MAX_CONNS=10

# --- Other Potential Variables (Example) ---
# LOG_LEVEL=debug
